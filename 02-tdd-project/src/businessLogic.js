/**
 * businessLogic.js
 * Regras de Negócio da TechStore
 *
 * ❌ NÃO use os operadores nativos (+, -, *, /) diretamente aqui.
 * ✅ OBRIGATÓRIO: importe e use as funções de coreMath.js.
 *
 * Fluxo TDD:
 *   1. Rode os testes → veja tudo VERMELHO
 *   2. Implemente UMA função por vez
 *   3. Rode novamente → veja ela ficar VERDE
 *   4. Repita até todos os testes passarem
 */

import { add, subtract, multiply, divide } from './coreMath.js';

export function calculateAverage(purchases) {
  // implemente aqui
}

export function calculateDiscountedPrice(price, discountPercent) {
  // implemente aqui
}
